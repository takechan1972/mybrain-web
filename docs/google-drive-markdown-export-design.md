# Google Drive Markdown 書き出し連携 ― 設計メモ

> **ステータス：設計のみ（未実装）。** このドキュメントは実装前の方針整理であり、アプリコード・OAuth・Drive API 呼び出しは含まない。
> 関連：`docs/obsidian-vault-export-design.md`（ローカル Vault 書き出し）／`lib/markdown/export-memos-zip.ts`／`lib/fs/`（ローカル FS コア）。

---

## 機能名

**Google Drive Markdown 書き出し連携**（Google Drive Markdown Export）

選択したメモを、ユーザーの Google Drive 上の `MyBrain/Memos/` に Obsidian 互換 Markdown（`.md`）として書き出す。

---

## 目的

- ローカル Vault 直接書き出しは Chromium 系デスクトップに限られる。**モバイルや非対応ブラウザでも**クラウド経由で Vault 互換ファイルを置けるようにする。
- 端末を問わず、メモを Obsidian 互換 Markdown として持ち出せる経路を増やす（「ハブアプリ」構想の一部）。
- Supabase は引き続き source of truth。Drive 書き出しは**コピーの一方向エクスポート**。

## なぜ Google Drive が有用か

- 主要 OS・ブラウザ・モバイルから広く使える（File System Access API の制約を受けない）。
- Obsidian の Vault を Google Drive 上に置いて同期しているユーザーが多く、**Drive に置けばそのまま Vault に反映**されるケースがある。
- 端末間共有・バックアップを Google 側に委ねられる。

## ローカル Vault 書き出しとの違い

| 観点 | ローカル Vault（File System Access） | Google Drive |
|---|---|---|
| 対応環境 | Chromium 系デスクトップのみ | デスクトップ／モバイル広く |
| 認証 | 不要（フォルダ選択のみ） | OAuth が必要 |
| 保存先参照 | `FileSystemDirectoryHandle`（IndexedDB 保存） | Drive のフォルダ ID（＋トークン） |
| ネットワーク | 不要（ローカル書き込み） | 必須（API 経由アップロード） |
| 既存ファイル判定 | ディレクトリ内のファイル存在チェック | Drive 検索（フォルダ ID ＋名前） |

両者の**コア（Markdown 化・重複名回避・件数確認・安全ルール）は共通化**し、書き出し先だけをアダプタとして差し替える。

---

## MVP スコープ

1. ユーザーが Google アカウントで連携（OAuth 同意）する導線。
2. `MyBrain/Memos/` フォルダを Drive 上に確保（無ければ作成）。
3. 選択メモを既存の `createMemoMarkdownFile(memo)` で Markdown 化（生成ロジックは再利用・複製しない）。
4. アップロード前に**件数を含む確認ダイアログ**を出す。
5. 同名ファイルがある場合は**上書きせず**連番（`名前-2.md`）で新規作成。
6. 結果（成功／失敗の件数、失敗タイトル）をトースト等で通知（ローカル書き出しと同じ方針）。
7. デスクトップ・モバイル双方で利用可能。

## スコープ外（このフェーズでやらないこと）

- 双方向同期（Drive → MyBrain の取り込み・競合解決）。
- 自動・定期アップロード（バックグラウンド同期）。
- Drive 上のメモ編集の読み戻し。
- 複数アカウント・複数フォルダの同時管理。
- Google Calendar / Sheets 連携（別機能）。
- Supabase スキーマ／保存挙動の変更。

---

## OAuth 概要（このユースケースで使う範囲）

- Google OAuth 2.0 で**ユーザー同意**を得てアクセストークンを取得する。
- Web アプリ向けのフロー（Authorization Code ＋ PKCE、または Google Identity Services のトークンフロー）を想定。
- **ユーザー操作起点**でのみ同意フローを開始（勝手にログインしない）。
- リフレッシュトークンを使う場合はサーバー側保管が前提（下記トークン方針）。MVP は最小権限・短命トークン中心で検討。

## 必要な Google API スコープ

- 第一候補：**`drive.file`**（このアプリが作成・オープンしたファイルのみアクセス）。
  - 最小権限。MyBrain が作った `MyBrain/Memos/` 配下だけ扱え、ユーザーの他のファイルには触れない。
- 広い `drive`（全ファイルアクセス）は**使わない**（過剰権限）。
- 既存 Vault フォルダへ正確に書き込む必要が出た場合のみ、`drive.file` の範囲で「ユーザーがピッカーで選んだフォルダ」を対象にする方式（Google Picker）を検討。

## トークン保管方針

- **保存する場合も最小限**。理想は**サーバー側（バックエンド）でのみリフレッシュトークンを保持**し、クライアントには短命のアクセストークンのみ渡す。
- クライアント（ブラウザ）には**リフレッシュトークンを保存しない**。アクセストークンもメモリ保持を基本とし、長期保存は避ける。
- localStorage への生トークン保存は**しない**（XSS 露出リスク）。
- 失効・取り消しに耐えるよう、トークン無効時は再同意フローに戻す。

### 保存するもの

- 連携状態（接続済みかどうか）の最小フラグ。
- 書き出し先フォルダの Drive フォルダ ID（再利用のため）。
- 任意で表示用のアカウント識別（メールの一部など、最小限）。

### 保存しないもの

- メモ本文・Markdown の中身（Supabase が source of truth）。
- Supabase のデータ。
- リフレッシュトークン（クライアント側には保存しない）。
- 個人情報（必要最小限を超える Google プロフィール情報）。

---

## Drive フォルダ構造（案）

ローカル Vault と**同じ構造**に揃える：

```
（Drive のルート or ユーザー指定フォルダ）/
└─ MyBrain/
   └─ Memos/
      ├─ 買い物メモ.md
      ├─ 会議メモ.md
      └─ 無題のメモ.md
```

- パス／ファイル名は既存の `createMemoMarkdownFile`（`MyBrain/Memos`、ファイル名規則）に揃え、ローカル書き出し・ZIP と結果一致。
- Drive ではフォルダは ID で扱うため、`MyBrain` → `Memos` の各フォルダ ID を解決／作成して保持する。

---

## Markdown 書き出しの挙動

1. 選択メモを `createMemoMarkdownFile` で `{ fileName, content }` 化。
2. `MyBrain/Memos/` フォルダ ID を確保（無ければ作成）。
3. ファイルごとに：
   - 既存チェック（フォルダ ID ＋ファイル名で Drive 検索）。
   - 無ければ作成してアップロード。
   - あれば**上書きせず**連番（`名前-2.md`）で新規作成。
4. 進捗・結果（成功／失敗の件数、失敗タイトル）を集計して通知。
5. 失敗があっても処理は止めず、最後にまとめて報告（ローカル書き出しと同方針）。

### 一方向書き出しのみ

- 本フェーズは **MyBrain → Drive の一方向**に限定。Drive 側の変更は読み戻さない。

### アップロード前のユーザー確認

- 複数ファイルをアップロードする前に、**必ず件数つき確認ダイアログ**を出す（`MyBrain/Memos/` 明記）。
- 大量選択時は警告（しきい値はローカルと揃え 10 件）。

### 黙ってアップロードしない

- ユーザー操作（クリック）起点でのみアップロードを実行。自動・定期アップロードはしない。

### 既存 Drive ファイルを黙って上書きしない

- 同名ファイルは上書きせず連番で新規作成（下記）。

### 重複ファイル名の扱い

- フォルダ内に同名がある場合、`名前-2.md` `名前-3.md` … と連番を付ける（ローカル・ZIP と同じ規則）。

---

## エラーハンドリング方針

| 事象 | 対応 |
|---|---|
| 未連携／同意していない | アップロード導線を無効化し、連携を案内 |
| OAuth 同意キャンセル | 何もしない（無音で終了） |
| トークン失効・取り消し | 再同意フローへ誘導し、処理は中断 |
| ネットワーク／API エラー | その1件を失敗計上し継続、最後に件数報告 |
| レート制限（429 等） | リトライ間隔を空ける／件数が多い場合は警告。無理に連投しない |
| フォルダ作成失敗 | 中止し、失敗を通知 |
| 想定外の例外 | 失敗トースト（既存文言に揃える） |

---

## モバイル・デスクトップの挙動

- **デスクトップ**：ローカル Vault 直接書き出し（実装済み）と Drive 書き出しの両方を提供しうる。
- **モバイル**：ローカル直接書き出しは非対応のため、**Drive 書き出しがクラウド経路の主役**になる（ZIP との併用）。
- どちらも OAuth 同意・件数確認・上書き回避の安全ルールは共通。

---

## Obsidian との関係

- Drive 上の `MyBrain/Memos/` は **Obsidian 互換 Markdown**（既存の YAML frontmatter ＋本文）で保存。
- Obsidian の Vault を Google Drive 同期している場合、Drive に置けば Vault に取り込まれうる。
- ローカル Vault 書き出しと**ファイル構造・命名を一致**させ、経路が違っても同じ結果にする。

## Google Calendar との関係

- 本機能は **Drive へのファイル書き出し**であり、Calendar 連携（予定の取得・登録）とは**別機能・別スコープ**。
- 認証基盤（Google OAuth）を将来共有しうるが、スコープは機能ごとに最小化する（Drive と Calendar のスコープを混ぜない）。

## Google Sheets との関係

- Sheets 連携（表形式エクスポート等）も**別機能・別スコープ**。
- Markdown 書き出しの対象外。共通化するのは OAuth 基盤の考え方までで、データ形式・保存先は別。

---

## OAuth 同意フロー設計

> **ステータス：設計のみ（未実装）。** ここではフローと方針だけを決める。OAuth 実装・トークン保存・Drive API 呼び出しはこのステップに含まない。

### 推奨アプローチ

- **MVP は Google Identity Services（GIS）のトークンフロー**（`google.accounts.oauth2.initTokenClient` による暗黙的なアクセストークン取得）を採用する。
- 理由：ブラウザだけで完結し、**リフレッシュトークンを扱わない**ため、クライアントにシークレットや長期トークンを置かずに済む。
- バックエンドの OAuth エンドポイント・リフレッシュトークン管理を実装するコストを MVP では負わない。

### GIS トークンフロー vs Authorization Code + PKCE

| 観点 | GIS トークンフロー（MVP 採用） | Authorization Code + PKCE |
|---|---|---|
| 取得物 | 短命のアクセストークンのみ | 認可コード → アクセス＋リフレッシュトークン |
| リフレッシュトークン | 受け取らない | 受け取る（保管が必要） |
| 必要な基盤 | クライアントのみ | トークン交換・保管のサーバ側が必要 |
| 再認証 | 失効時に再度トークン要求 | リフレッシュで自動更新 |
| MVP 適性 | ◎（最小・安全） | △（基盤コスト大） |

- 将来、無音の自動アップロードや長期セッションが必要になった場合に限り、**サーバ側 Authorization Code + PKCE** へ移行を検討する。

### MyBrain MVP で優先する方式

- **GIS トークンフロー**を優先。アクセストークンは**メモリ保持**を基本とし、書き出し操作のたびにユーザー操作起点で取得／再取得する。

### なぜ MVP はブラウザにリフレッシュトークンを保存しないか

- リフレッシュトークンは長期間有効で、漏えい時の被害が大きい。
- ブラウザ（localStorage / IndexedDB / Cookie）に置くと **XSS で奪取されるリスク**がある。
- MVP は短命アクセストークンのみを使い、長期トークンを**そもそもクライアントに渡さない**ことでリスクを下げる。

### 必要なスコープ

- **`https://www.googleapis.com/auth/drive.file`** のみ。
  - アプリが作成・オープンしたファイルだけにアクセスでき、ユーザーの他の Drive ファイルには触れない（最小権限）。
- 広い `drive`（全ファイル）は要求しない。

### リダイレクト URI の考慮

- GIS トークンフローはポップアップ／トークンコールバック中心で、従来型のリダイレクト URI 登録は最小限。
- それでも **承認済みの JavaScript 生成元（Authorized JavaScript origins）** に本番・プレビュー・localhost を登録する必要がある。
- Authorization Code 方式へ移行する場合は、別途**承認済みリダイレクト URI**（サーバのコールバック）が必要になる。

### 同意画面の挙動

- **ユーザー操作（クリック）起点**でのみ同意フローを開始する（自動で開かない）。
- 初回は Google の同意画面でスコープ（`drive.file`）への許可を求める。
- ユーザーがキャンセルした場合は何もしない（無音で終了）。
- アプリ公開状態（テスト／本番）に応じて、未確認アプリの警告が出る点に留意。

### トークンの寿命

- アクセストークンは**短命（おおむね約1時間）**。
- MVP ではリフレッシュトークンを持たないため、失効したら**再度ユーザー操作でトークンを取得**する。

### アクセストークンの取り扱い

- **メモリ（実行中の変数）で保持**し、アップロード時に使う。
- 長期保存しない。タブを閉じたら破棄でよい（次回操作時に再取得）。
- ログや URL に載せない。

### リフレッシュトークンの取り扱い

- **MVP では取得しない／保持しない。**
- 将来サーバ側方式を導入する場合に限り、**サーバでのみ暗号化保管**し、クライアントには渡さない。

### サーバ側トークン保管方針（将来）

- リフレッシュトークンを使うならサーバ側に限定し、暗号化・アクセス制御・失効処理を備える。
- クライアントへはその都度の短命アクセストークンだけを渡す。

### クライアント側トークン保管方針

- アクセストークンはメモリのみ。
- **リフレッシュトークン・クライアントシークレットはクライアントに置かない。**

### localStorage に絶対に保存しないもの

- アクセストークン（生）。
- リフレッシュトークン。
- クライアントシークレット。
- ユーザーの個人情報や Drive のファイル内容。

### トークン失効時の挙動

- API が 401 等を返したら、保持中のアクセストークンを破棄。
- ユーザーに再連携を促し、**ユーザー操作起点で再度トークン取得**してから処理を続ける。
- 失効中はアップロードを実行しない（黙って失敗させない）。

### 将来の「接続解除」の挙動

- 保持中のアクセストークンを破棄し、（あれば）GIS の `revoke` でトークンを取り消す。
- ローカルに保存している連携フラグ・Drive フォルダ ID を削除する。
- 解除は**ローカルの参照とトークンを消すだけ**で、Drive 上の既存ファイルや Supabase のデータには触れない。
- ローカル Vault の `clearVaultHandle()` 相当の考え方に揃える。

### セキュリティ上の注意

- 最小スコープ（`drive.file`）を厳守する。
- トークンは短命・メモリ保持・非永続を基本にする。
- すべての連携・アップロードはユーザー操作起点（自動実行しない）。
- XSS 対策（トークンを DOM/localStorage に晒さない）を前提にする。
- Supabase を source of truth に保ち、Drive は一方向のコピー先にとどめる。

### このステップについて（OAuth）

- **本ステップはドキュメントのみ。** OAuth 実装・トークン保存・Drive API 呼び出し・依存追加・UI 変更・Supabase 変更は行わない。
- 実装は別フェーズで、まず「OAuth 開始ヘルパー（UI 非接続・トークンはメモリ）」から段階的に進める想定。

---

## 将来の双方向同期について（注記のみ）

- 本フェーズおよび Drive 書き出しは**一方向（MyBrain → Drive）**に限定。
- 双方向同期（Drive の編集を MyBrain へ取り込む）は、競合解決・更新時刻比較・削除の扱いなど別途設計が必要で、**現時点ではスコープ外**。
- 双方向に進む場合も、Supabase を source of truth とする前提を崩さない設計から検討する。

---

## このステップについて

- **本ステップはドキュメントのみ。** OAuth・Drive API 呼び出し・依存追加・UI 変更・Supabase 変更は行わない。
- 実装は別フェーズで、まず「対応検出・連携状態の最小ヘルパー（UI 非接続）」から段階的に進める想定。

---

## 実機テスト結果（手動エクスポート）

> 上記の設計は「設計のみ」と記載しているが、その後のフェーズで**手動エクスポート経路は実装・UI 接続済み**（`lib/google/google-drive-export.ts` の `exportMemosToGoogleDrive` ＋ `components/DesktopMemos.tsx` の「Google Driveへ書き出し」ボタン）。以下はその**本番（Vercel）での実機確認結果**。

- **テスト日**：2026-07-01
- **環境**：
  - 本番（Vercel）: `https://mybrain-web.vercel.app`
  - Vercel に `NEXT_PUBLIC_GOOGLE_DRIVE_ENABLED=true` / `NEXT_PUBLIC_GOOGLE_DRIVE_CLIENT_ID`（実 Client ID）設定済み・再デプロイ済み

### 実施フロー（確認済み）

- 本番の `/memos` で「**Google Driveへ書き出し**」ボタンが**表示された**。
- ボタンを**クリックした**。
- Google Drive への書き出し／保存が**成功した**。
- 既存の**手動エクスポート経路が本番で動作することを確認**した。
- これは**手動エクスポート**であり、保存先 `obsidian-gdrive` の**自動保存ではない**。

### 結果サマリ

| テスト | 結果 |
|---|---|
| 本番でボタン表示 | ✅ Pass |
| ボタンクリック → Google 認可 | ✅ Pass |
| Google Drive へ書き出し成功 | ✅ Pass |

### ボタン表示までの操作（UX メモ）

ボタンはメモ一覧の**選択フロー**からのみ出る（詳細画面には出ない）：

1. **メモ一覧へ戻る**（詳細を開いている場合）
2. **選択してまとめる**（選択モードに入る）
3. **メモを1件以上選択**
4. **Google Driveへ書き出し**

### 備考

- **MyBrain/Supabase が引き続き source of truth**。Google Drive 書き出しは一方向コピー（best-effort）。
- Google Drive 書き出しは**ユーザー操作（クリック）起点の手動エクスポート**。自動・定期アップロードはしない。
- 保存先 `obsidian-gdrive`（自動保存）は**まだ未実装**（自動書き出しはしない・プレースホルダのまま）。
- 認可は GIS トークンフロー（scope `drive.file`）・短命アクセストークン（メモリのみ）。

---

## 実機テスト結果（詳細画面からの手動エクスポート）

> その後のフェーズ（G6）で、メモ**詳細画面**にも単体エクスポートボタン「Google Driveへ書き出し」を追加（`components/DesktopMemos.tsx`）。一覧の選択フローに戻らずに、表示中の 1 件をそのまま書き出せる。以下はその**本番（Vercel）での実機確認結果**。

- **テスト日**：2026-07-01
- **環境**：
  - 本番（Vercel）
  - Google Drive env 設定済み
  - デスクトップのメモ**詳細画面**

### 実施フロー（確認済み）

- 詳細画面に「**Google Driveへ書き出し**」ボタンが**表示された**。
- ボタンを**クリックした**。
- Google Drive への書き出し／保存が**成功した**。
- Drive の `MyBrain/Memos/` 配下に **Markdown ファイルが生成された**。

### 結果サマリ

| テスト | 結果 |
|---|---|
| 詳細画面でボタン表示 | ✅ Pass |
| ボタンクリック → 書き出し成功 | ✅ Pass |
| `MyBrain/Memos/` に .md 生成 | ✅ Pass |

### 備考

- これも**ユーザー操作（クリック）起点の手動エクスポート**。
- **MyBrain/Supabase が引き続き source of truth**。
- Google Drive 書き出しは既存ファイルを**上書きしない**。同名がある場合は `-2.md` / `-3.md` のように連番ファイルを新規作成する（現行の手動エクスポート仕様どおり・期待挙動）。
- 保存先 `obsidian-gdrive`（自動保存）は**まだ未実装**。
